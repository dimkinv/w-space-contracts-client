import { ToCorporationImagePipe } from './to-corporation-image.pipe';

describe('ToCorporationImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ToCorporationImagePipe();
    expect(pipe).toBeTruthy();
  });
});
