import { ToCharacterImagePipe } from './to-character-image.pipe';

describe('ToCharacterImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ToCharacterImagePipe();
    expect(pipe).toBeTruthy();
  });
});
