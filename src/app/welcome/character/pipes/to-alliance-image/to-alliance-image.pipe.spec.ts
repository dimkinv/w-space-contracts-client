import { ToAllianceImagePipe } from './to-alliance-image.pipe';

describe('ToAllianceImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ToAllianceImagePipe();
    expect(pipe).toBeTruthy();
  });
});
